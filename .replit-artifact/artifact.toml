kind = "api"
previewPath = "/"
title = "Taylor Invoices / BooksIQ"
version = "1.0.0"

[[services]]
localPort = 3000
name = "Taylor Invoices Dev Server"
paths = ["/", "/api"]

[services.development]
run = "node dev-server.js"

[services.env]
PORT = "3000"
