# BooksIQ QA Report — 2026-05-03

End-to-end audit covering: Drive integration, auth/sessions, frontend navigation, and sheets-sync API. Four parallel structural audits ran; findings triaged into "fix tonight" vs "Xero-clone-roadmap material" (Stage 0 Postgres migration solves most architectural issues for free).

## Tier 1 — Fixed and shipped tonight

| # | Issue | File:line | Fix |
|---|---|---|---|
| 1 | Invoice doubling on cloud reload (5 logos became 10, etc.) | `public/index.html` ~3204 | Replaced two-call stamp logic with dedup-on-ingest using `Set` keyed by entry id and `num\|entity`. Entity now resolved from row's own `entity`/`logoType`. **Shipped earlier, code-review APPROVED, live verified.** |
| 2 | `saveQuickSvc` saved drafts only to localStorage — lost on cache clear | `public/index.html:7118` | Added `syncPracticesToCloud()` follow-up matching siblings. |
| 3 | `syncEntries` (bulk "Sync to cloud") didn't dual-write to user's backup sheet | `api/sheets-sync.js:1234` | Refactored into `writeToSheet` helper, now writes both primary + backup. |
| 4 | `syncInvoices` same | `api/sheets-sync.js:1611` | Same pattern. |
| 5 | `syncSettings` same | `api/sheets-sync.js:1922` | Added backup mirror block at end. |
| 6 | Sessions could be created with empty `sheetId`, causing silent fallback to env-var sheet | `api/auth/verify.js:54`, `api/auth/verify-code.js:130` | Hard fail with "Account setup incomplete" if `user.sheetId` missing. |

## Tier 2 — Deferred to Xero clone Stage 0 (Postgres)

These are real architectural issues, but bandaiding the Sheets-as-DB layer wastes effort because the planned Postgres migration eliminates them by construction. All tracked in `XERO_CLONE_ROADMAP.md`.

| Issue | File:line | Resolved by |
|---|---|---|
| Read-modify-write race conditions on every update path (`updateEntry`, `updateInvoice`, `batchUpdateEntries`, `syncSettings`) | `api/sheets-sync.js:735, 813, 1335, 1408, 1950` | Postgres + transactions (Stage 0) |
| No tab-existence verification before writes | `api/sheets-sync.js` (write paths) | Postgres schema (Stage 0) |
| Onboarding not atomic — partial failures leave orphaned Drive folders or unregistered users | `api/onboarding/submit.js:182` | Re-implemented with DB transaction (Stage 0) |
| Client-side invoice number allocation can collide across devices | `public/index.html:6297, 6390, 6468` | Server-side allocator with sequence (Stage 1 ledger) |
| "Mark as paid" doesn't re-stamp the PDF | `api/sheets-sync.js:1408` | Stage 8 reports/PDFs |
| No restore-from-trash UI (data is preserved in Trash tab, but no admin/user path to undelete) | n/a | Stage 9 polish |
| Apps Script `InvoicePDFGenerator.gs` legacy path doesn't mirror PDFs to backup folder | `InvoicePDFGenerator.gs` | Apps Script gets retired when all users migrated |
| Read-only admin overrides not audited (write actions are) | `api/sheets-sync.js:632` | Stage 0 audit log |
| `withStats=true` admin user list does N sequential Sheets reads (quota risk) | `api/admin/users.js:106` | Single SQL query (Stage 0) |

## Audit false positives (already correct in code)

The audits flagged these but reading source confirmed the code is fine:

- `await drive.permissions.create(...)` — already awaited (`api/drive-upload.js:156`)
- `await mirrorInvoiceToMaster(...)` — already awaited (`api/sheets-sync.js:1306`)
- `await deleteMirroredInvoice(...)` — already awaited (`api/sheets-sync.js:1150`)
- `await deleteMirroredEntry(...)` — already awaited (`api/sheets-sync.js:1006`)
- `saveServiceDuration` — already calls `await syncSettingsToCloud()`
- `saveAhPracSettings` — already calls `syncSettingsToCloud()` + `syncPracticesToCloud()`
- `saveDraftService` / `deleteDraftService` — already call `await syncSettingsToCloud()`
- `saveInvoiceMetadataOnly` — already calls `apiCall('sheets-sync', { action: 'update_invoice' })`
- `addSvcToAhPrac` — already syncs

## Page-by-page navigation audit

All top-level views render and route correctly:

| View | Handler | Status |
|---|---|---|
| Home | `showScreen('home')` | ✓ |
| Archive | `showScreen('archive')` | ✓ |
| Invoices | `showScreen('invoices')` | ✓ |
| Tracking | `showScreen('tracker')` | ✓ |
| Analytics | `showScreen('analytics')` | ✓ |
| Settings | `showScreen('settings')` | ✓ |
| Admin | `showScreen('admin')` | ✓ |
| Practice | `openPractice(id)` | ✓ |
| Ad Hoc | `showScreen('adhoc')` | ✓ |

No undefined onclick handlers found in active code.

## Auth / multi-user

- Magic-link TTL: 15 min, single-use enforced via `MagicLinks.used` column ✓
- Rate limiting: 3/email/15m, 10/IP/1h on request; 5/email/15m, 30/IP/15m on verify ✓
- Admin override (`X-Override-Sheet-Id`): `requireSession` re-checks role against Master Sheet every 60s, header is honoured only for `role==='admin'` ✓
- `master_override_login` correctly fetches the target user's full row from Master Sheet — no leak of admin's own credentials ✓

## Outstanding ops items (not code fixes)

- Set `GOOGLE_DRIVE_HIDDEN_BACKUP_ROOT_ID` on Vercel so onboarding-time backup folders are created in the right hidden root
- Have Taylor and Reo log out + back in to clear any pre-fix sessions
